apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mailings.fullname" . }}-test-connection"
  labels:
    {{- include "mailings.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mailings.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
