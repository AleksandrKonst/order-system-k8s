apiVersion: v1
kind: Service
metadata:
  name: {{ include "mailings.fullname" . }}
  labels:
    {{- include "mailings.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mailings.selectorLabels" . | nindent 4 }}
